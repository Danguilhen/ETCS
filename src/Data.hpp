#ifndef DATA
#define DATA

#include "define.hpp"

class Data
{
	private :

		/*int sock = socket(AF_INET, SOCK_STREAM,0);
		int socketValue = 0;*/
		//void SocketSend(char buf[]);


	public :
		Data();
		void update();
};

#endif